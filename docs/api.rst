API Reference
=============

The stable SDK facade is intentionally small. The implementation classes are
re-exported so embedding applications can use the same durable stores as the
desktop application.

.. automodule:: unifile_sdk
   :members:
   :undoc-members:
